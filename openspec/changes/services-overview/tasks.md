## 1. Shared copy and links

- [ ] 1.1 Extract the five service `{ label, blurb }` entries to a shared module used by homepage and `/services`
- [ ] 1.2 Point nav Services and homepage service teasers at `/services`

## 2. Overview page

- [ ] 2.1 Add `/services` with Task Force + Services document title, serif H1, intro copy
- [ ] 2.2 Render five Neutral Cards (mist, 24px radius) from the shared list; no `/services/[slug]` routes
- [ ] 2.3 Add “Start a Project” pill to `/contact`

## 3. Verify

- [ ] 3.1 Confirm `/services` loads locally without blocking errors; nav and homepage teasers land on it
