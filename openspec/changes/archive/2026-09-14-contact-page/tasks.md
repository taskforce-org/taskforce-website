## 1. Content document

- [x] 1.1 Add `src/lib/contact.ts` with typed `contactContent`: documentTitle, description, heading, intro, availability, estimates, nextStep, callout, cta label “Start a Project” and href `/contact`. Dummy copy in the homepage studio voice; no lorem, numeric prices, SLAs, calendar guarantees, phone numbers, street addresses, named people, or invented email domains
- [x] 1.2 Leave nav Contact on `/contact`. Leave FAQ on `/faq`, Process on `/process`, Studio on `/studio`, footer Technology on `/technology`

## 2. Contact page

- [x] 2.1 Replace `/contact` stub so the page reads only `contactContent` for metadata and body copy (no extra Contact strings in the page file)
- [x] 2.2 Render heading, intro, availability / estimates / next-step cards (mist 24px), peach callout, “Start a Project” pill from the document. Stay on Steep; at most one peach card. Do not add a form

## 3. Verify

- [x] 3.1 Confirm `/contact` loads locally on port 3000, document title includes Task Force and Contact, Contact nav and Start a Project land on it, no inquiry form, FAQ/Process/Studio/footer Technology still land on `/faq` `/process` `/studio` `/technology`
